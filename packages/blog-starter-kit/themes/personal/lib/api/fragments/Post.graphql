fragment Post on Post {
	id
	title
	subtitle
	url
	author {
		name
		profilePicture
	}
	coverImage {
		url
	}
	publishedAt
	slug
	brief
	readTimeInMinutes
	tags {
		id
		name
		slug
	}
	series {
		id
		name
		slug
	}
	comments(first: 0) {
		totalDocuments
	}
}
