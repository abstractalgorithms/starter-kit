fragment Post on Post {
	id
	title
	subtitle
	url
	author {
		name
		profilePicture
	}
	coverImage {
		url
	}
	publishedAt
	updatedAt
	slug
	brief
	readTimeInMinutes
	views
	tags {
		id
		name
		slug
	}
	series {
		id
		name
		slug
	}
	comments(first: 0) {
		totalDocuments
	}
}
